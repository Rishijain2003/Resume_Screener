# Resume Screener UI (Next.js)

```bash
npm install
npm run dev
```

Dev server runs on **port 1830** (`npm run dev` → http://localhost:1830).

Set `NEXT_PUBLIC_API_URL` in `.env.local` if the API is not at `http://127.0.0.1:8000`.
