/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  /** Static HTML/JS in `out/` for hosts like Render Static Sites (no Node server). */
  output: "export",
};

export default nextConfig;
